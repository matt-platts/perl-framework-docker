/* Settings */
/* Note that config values are populated from the config/ api call from values in the back end config file by an ajax call.
 * The API_PATH is required here however to make that initial call.
 */

var ENVIRONMENT // console logging is turned off in PRODUCTION mode
var API_PATH = "/portal-api/";
var ALLOW_EDITING;


